# To enable / disable self-hosting features.
SELF_HOSTED=false

# Enable Synth for securities/stock data (careful, this will use your API credits)
# Only needed if you track investment accounts. Exchange rates use free providers (no key needed).
SYNTH_API_KEY=yourapikeyhere
